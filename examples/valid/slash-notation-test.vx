(macro defn [name params body]
  (def ~name (fn ~params ~body)))

(import "net/http")
(import "github.com/gorilla/mux")
(import "fmt")

(defn hello-handler [w r]
  (.WriteString w "Hello from slash notation Vex!"))

(def router (mux/NewRouter))
(.HandleFunc router "/" hello-handler)
(fmt/Println "🚀 Slash notation Vex HTTP Server starting on :8080...")
(http/ListenAndServe ":8080" router)